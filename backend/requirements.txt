# UFDR Investigator Backend Dependencies - Phase 2: Storage & Indexing
# Python 3.11+

# Core Dependencies
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0

# Search
opensearch-py>=2.1.0

# Development (optional)
pytest>=7.0.0
black>=23.0.0
isort>=5.12.0

# For SQLite development fallback (included in standard library but listing for clarity)
# sqlite3 - included in Python standard library